# **Q3. REPEATABLE READ**

MySQL InnoDB의 기본 격리 수준인 REPEATABLE READ에서 Phantom Read가 발생하지 않는 이유를 설명하세요.

---

InnoDB는 `NEXT KEY LOCK`으로 `REPEATABLE READ`를 보장하기 때문에 `PHANTOM READ`가 발생하지 않는다.

데이터를 조회 할 때, 대상 인덱스 범위에 레코드 락이 걸리고,
그 외의 범위에는 갭 락이 걸려서 새로운 데이터가 Insert 되는 것을 막는다.